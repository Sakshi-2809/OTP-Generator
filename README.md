# 🔒 OTP Generator (JavaScript)

A simple and stylish **Random OTP (One-Time Password) Generator** built with HTML, CSS, and JavaScript.

Click the **Get OTP** button to generate a random 4-digit OTP displayed on the screen with a neon glow effect.

---

## 📝 About the Project

This project demonstrates the use of JavaScript’s `Math.random()` and DOM manipulation to generate and display a random OTP.

It is a great project for understanding:
- Generating random numbers in JavaScript
- Basic DOM manipulation
- Applying dynamic styles with JavaScript

---

## ✨ Features

- Generates a random 4-digit OTP
- Neon-themed UI with glowing text and button effects
- Lightweight and fully client-side
- Easy to use and responsive

---

## 🛠️ Technologies Used

- **HTML5** – Webpage structure  
- **CSS3** – Styling and neon effects  
- **JavaScript** – OTP generation and dynamic DOM updates

---

## ⚙️ How It Works

1. The user clicks the **Get OTP** button
2. JavaScript generates a random number between 0 and 9999
3. The number is rounded down to the nearest integer
4. OTP is displayed on the screen with neon glow styling applied dynamically

---

## 📂 Project Structure

```text
OTP-Generator/
│
├── index.html   # HTML structure
├── otp.css      # Styling and neon effects
├── otp.js       # OTP generation logic
└── README.md
